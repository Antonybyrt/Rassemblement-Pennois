# Configuration SMTP pour l'envoi d'emails
# Remplacez ces valeurs par vos propres paramètres SMTP

# Serveur SMTP (exemples courants)
SMTP_HOST=smtp.gmail.com
# ou pour Outlook: smtp-mail.outlook.com
# ou pour Yahoo: smtp.mail.yahoo.com
# ou pour un hébergeur: mail.votre-hebergeur.com

# Port SMTP
SMTP_PORT=587
# 587 pour TLS (recommandé)
# 465 pour SSL
# 25 pour non sécurisé (non recommandé)

# Sécurité SMTP
SMTP_SECURE=false
# true pour le port 465 (SSL)
# false pour le port 587 (TLS)

# Identifiants SMTP
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre-mot-de-passe-app

# Email d'expéditeur (peut être différent de SMTP_USER)
SMTP_FROM="contact@mail.com"

# Email de destination pour les messages de contact
CONTACT_EMAIL=contact@mail.com

# Configuration de l'environnement
NODE_ENV=development
